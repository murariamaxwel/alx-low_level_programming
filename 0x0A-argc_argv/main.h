#ifndef MAIN_H
#define MAIN_H

/**
 * _putchar - writes the character c to standard output
 * @c: Reprsents the character to print
 *
 * Return: On success, return 1.
 * On error, -1 should be returned
 */

int _putchar(char c);

#endif
