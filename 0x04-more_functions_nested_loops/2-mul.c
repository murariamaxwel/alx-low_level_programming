#include "main.h"

/**
 * mul - takes two numbers and finds their product
 * @a: one of the numbers
 * @b: the other number
 * Return: product of a and b
 */

int mul(int a, int b)
{
	return (a * b);

}
